<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="counsel">
     
     <select id="selectCounselList" parameterType="hashmap" resultType="hashmap">
     	<![CDATA[
          SELECT STUDENT_NO
				,NM
				,STAGE
				,VISIT_DATE
				,VISIT_TIME
				,PHONE_NO
				,CLSS
				,RMK
				,STATUS
				,CLS_DETAIL
			FROM COUNSEL
		ORDER BY VISIT_DATE DESC, VISIT_TIME ASC;
        ]]>
     </select>
     
</mapper>